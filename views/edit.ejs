<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <form action="/edit/<%= book.id %>" method="POST">
  <label for="title">Title:</label>
  <input type="text" name="title" value="<%= book.title %>" required />

  <label for="author">Author:</label>
  <input type="text" name="author" value="<%= book.author %>" />

  <label for="rating">Rating:</label>
  <select name="rating">
    <% for (let i = 1; i <= 5; i++) { %>
      <option value="<%= i %>" <%= book.rating == i ? 'selected' : '' %>><%= i %></option>
    <% } %>
  </select>

  <label for="read_date">Read Date:</label>
  <input type="date" name="read_date" value="<%= book.read_date ? book.read_date.toISOString().split('T')[0] : '' %>" />

  <label for="notes">Notes:</label>
  <textarea name="notes"><%= book.notes %></textarea>

  <label for="cover_id">Cover ID:</label>
  <input type="text" name="cover_id" value="<%= book.cover_id %>" />

  <button type="submit">Update Book</button>
</form>
</body>
</html>